<script>
  import "../styles/fonts.css";
</script>

<style is:global>
  :root {
    --section-padding: 4em;

    --radius: 1em;

    --subtitle: clamp(1.5rem, 5vw, 2.5rem);
    --title: clamp(2rem, 5vw, 3rem);
    --body: clamp(1rem, 5vw, 1.1rem);
    --badge: clamp(0.75rem, 5vw, 1rem);
  }

  @media screen and (min-width: 768px) {
    :root {
      --section-padding: 6em;

      --subtitle: clamp(1.5rem, 5vw, 2.5rem);
      --title: clamp(3rem, 5vw, 4rem);
      --body: clamp(1.25rem, 5vw, 1.4rem);
    }
  }

  body {
    background-color: #0a0a0a;
    margin: 0;
  }

  * {
    color: white;
    font-family: "General Sans", system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans",
      "Helvetica Neue", sans-serif;
    font-weight: 500;
  }

  *::selection {
    background-color: #333;
  }

  .container {
    padding: var(--section-padding);
    padding-top: 0;
    position: relative;
    overflow: hidden;
  }

  :not(.container) + .container {
    padding-top: var(--section-padding);
  }

  p,
  span {
    font-size: var(--body);
    max-width: 920px;
    line-height: 1.5;
  }

  a {
    text-decoration: none;
    border: 1px solid white;
    padding: 0 2.5px;
  }

  code {
    font-family: "JetBrains Mono", "Courier New", Courier, monospace;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-weight: 600;
  }

  @keyframes float {
    0% {
      transform: translateY(var(--min-range, -0.25rem)) rotate(var(--rotate, 0));
    }
    50% {
      transform: translateY(var(--max-range, 0.25rem)) rotate(var(--rotate, 0));
    }
    100% {
      transform: translateY(var(--min-range, -0.25rem)) rotate(var(--rotate, 0));
    }
  }

  .subtitle {
    font-size: var(--subtitle);
    margin: 0;
  }

  .title {
    font-size: var(--title);
    margin: 0;
  }

  .badge {
    font: var(--badge);
    width: fit-content;
    background-color: #0a0a0a88;
    padding: 0.2em 0.5em;
    border-radius: 0.5em;
    backdrop-filter: blur(0.5em);
  }

  .sprinkle {
    position: absolute;
    font-family: "JetBrains Mono", "Courier New", Courier, monospace;
    opacity: 0.15;
    user-select: none;
    animation: float 3s ease-in-out infinite;
    z-index: -1;
  }

  @media (prefers-reduced-motion: reduce) {
    .sprinkle {
      animation: none;
    }
  }
</style>
