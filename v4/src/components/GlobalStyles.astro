<script>
  import "../styles/fonts.css";
</script>

<style is:global>
  :root {
    --section-padding: 4em;

    --subtitle: clamp(1.5rem, 5vw, 2.5rem);
    --title: clamp(2rem, 5vw, 3rem);
    --body: clamp(1rem, 5vw, 1.1rem);
  }

  @media screen and (min-width: 768px) {
    :root {
      --section-padding: 6em;

      --subtitle: clamp(1.5rem, 5vw, 2.5rem);
      --title: clamp(3rem, 5vw, 4rem);
      --body: clamp(1.25rem, 5vw, 1.4rem);
    }
  }

  body {
    background-color: #0a0a0a;
    margin: 0;
  }

  * {
    color: white;
    font-family: "General Sans", system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans",
      "Helvetica Neue", sans-serif;
    font-weight: 500;
  }

  *::selection {
    background-color: #333;
    text-decoration: dotted;
  }

  .container {
    padding: var(--section-padding);
  }

  p,
  span {
    font-size: var(--body);
    max-width: 920px;
    line-height: 1.5;
  }

  a {
    text-decoration: none;
    border: 1px solid white;
    padding: 0 2.5px;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-weight: 600;
  }
</style>
